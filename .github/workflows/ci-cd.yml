name: CI/CD Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

permissions:
  contents: write


jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    
      - name: Checkout Code
        uses: actions/checkout@v3

      
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      
      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

   
      - name: Build and Push Docker Image
        uses: docker/build-push-action@v4
        with:
          context: .
          push: true
          tags: |
            vaishnavyejju/portfolio:latest
            vaishnavyejju/portfolio:${{ github.sha }}

     
      - name: Configure Git
        run: |
          git config user.name "vaishnavyejju"
          git config user.email "vaishnavyejju@gmail.com"

   
      - name: Update Helm values.yaml
        run: |
          sed -i "s|tag:.*|tag: ${{ github.sha }}|" ./pipeline-for-portfolio/values.yaml
          
          if git diff --quiet; then
            echo "❌ No changes detected — tag not updated"
            exit 1
          fi
      
          git add ./pipeline-for-portfolio/values.yaml
          git commit -m "Update Helm values.yaml with new image tag"
          git push



      
