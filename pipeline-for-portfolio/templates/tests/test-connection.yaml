apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pipeline-for-portfolio.fullname" . }}-test-connection"
  labels:
    {{- include "pipeline-for-portfolio.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pipeline-for-portfolio.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
